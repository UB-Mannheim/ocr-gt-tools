language: perl
dist: trusty
sudo: required

before_install:
  - sudo apt-get update -q
  - make deps
  - make dev-deps

install:
  - npm install
  - bower install

script:
  - make vendor
  - make dist
  - cp conf/ocr-gt-tools.ini_tmpl conf/ocr-gt-tools.ini
  - bash ./test.sh
